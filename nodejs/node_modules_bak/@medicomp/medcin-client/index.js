/**
 * Created by pkarcher on 12/12/2014.
 */
'use strict';

var medicomp = medicomp || (function() {

    var Enums = require('./enums');
    var Socket = require('./medcinSocket');
    var SocketBuffer = require('./medcinSocketBuffer');
    var ResultBuffer = require('./medcinResultBuffer');
    var CachedCodes = require('./medcinCachedCodes');
    var Validator = require('./medcinValidator');
    var Client = require('./medcinClient');
    var Configuration = require('./configuration');
    var Connection = require('./connection');
    return {
      Client : Client,
      Configuration : Configuration,
      Connection : Connection,
      Socket : Socket,
      SocketBuffer : SocketBuffer,
      ResultBuffer : ResultBuffer,
      CachedCodes : CachedCodes,
      Validator : Validator,
      Enums : Enums
    };
  }());

//medicomp.Connection = require("./connection");

module.exports = medicomp;

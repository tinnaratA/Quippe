/*** Created by pkarcher on 12/17/2014.*/
'use strict';

var medicomp = medicomp || {};

medicomp.Client = medicomp.Client || function(seedConfig) {

  var Connection = require('./connection');
  // configuration object is responsible for taking any wierd stuff the caller may or
  // may not have passed in and making sure a good (if possible) configuration object results.
  var configuration = require('./configuration')(seedConfig);

  this.getConnection = function(callbackFunction) {
        return new Connection(configuration, callbackFunction);
        };
  this.configuration = configuration;

  //return {
  //  configuration : configuration,
  //  getConnection : function(err, success) {
  //    return new Connection(configuration, err, success);
  //    }
  //};
};

module.exports = medicomp.Client;